// import { Hook } from "./avg-hooks";
// import { Subject } from "rxjs";

export class HookManager {
//   private hooks: Map<string, Subject<any>> = new Map<string, Subject<any>>();
//   public registerHook(name: string, cb: (data: any) => void) {
//     let subject = this.hooks.get(name);
//     if (subject) {
//       subject.subscribe(cb);
//     } else {
//       subject = new Subject();
//       subject.subscribe(cb);

//       this.hooks.set(name, subject);
//     }
//   }

//   public triggerHook(name: string, data: any) {
//     let subject = this.hooks.get(name);
//     if (subject) {
//       subject.next(data);
//     }
//   }
}
