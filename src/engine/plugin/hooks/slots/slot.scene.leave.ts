import { SpriteAnimationMacro } from "engine/core/graphics/sprite-animate-director";

const sceneLeaveAnimationSlot: SpriteAnimationMacro = {
  timeline: [
    {
      alpha: 0,
      duration: 600
    }
  ]
};

export default sceneLeaveAnimationSlot;
