import { AnimationMacro } from "engine/core/graphics/sprite-animate-director";

const sceneLeaveAnimationSlot: AnimationMacro = {
  timeline: [
    {
      alpha: 0,
      duration: 600
    }
  ]
};

export default sceneLeaveAnimationSlot;
