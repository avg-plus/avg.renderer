import * as http from "http";

export enum AVGRemoteMethod {
    GET,
    POST
}

export class AVGRemote {

    public static http(method: AVGRemoteMethod, url: string, data: Map<String, any>, [callback]) {
        
    }
}
